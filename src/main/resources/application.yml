server:
  port: 8081
  
logging:
  level:
    org.springframework.web.client.RestTemplate: DEBUG
    org.springframework.security: DEBUG
spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: oidc-cookie
            client-secret: hJi8KEDNjGJ6W5PKE93HwgLx28UkTRAE
        provider:
          keycloak:
            issuer-uri: http://127.0.0.1:8080/auth/realms/heroes
